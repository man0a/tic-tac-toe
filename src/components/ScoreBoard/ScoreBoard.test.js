import ScoreBoard from "./ScoreBoard"

it('renders without crashing', () => {
  shallow(<ScoreBoard />);
//  Check initial state
});


it('Updates the score from the score', () => {
  shallow(<ScoreBoard />);
//  Check initial state
});

it('Does not change score if stalemate', () => {
  shallow(<ScoreBoard />);
//  Check initial state
});